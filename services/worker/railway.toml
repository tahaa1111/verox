[build]
builder = "DOCKERFILE"
dockerfilePath = "services/worker/Dockerfile"
buildContext = "."

[deploy]
startCommand = "celery -A services.worker.celery_app worker --loglevel=info --queues=inference,default --concurrency=1"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
