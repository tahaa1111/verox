# =============================================================================
# DEPRECATED — Worker is now merged into the API service (single process).
# This Dockerfile is no longer used. The active Dockerfile is:
#   services/api/Dockerfile
#
# Keeping this file to avoid Railway picking up an empty directory.
# Railway deployment is configured via services/api/railway.toml only.
# =============================================================================
FROM python:3.11-slim
RUN echo "Worker merged into API service. See services/api/Dockerfile." && exit 1
