celery[redis]==5.4.0
redis[hiredis]==5.0.8
psycopg2-binary==2.9.9
Pillow==10.4.0
rapidfuzz==3.9.7
json-repair==0.28.1
python-dateutil==2.9.0
structlog==24.4.0
tenacity==9.0.0
httpx==0.27.2
# NER (EntityRuler — no model download needed, uses spacy.blank("fr") only)
spacy==3.7.4
# Spell correction (custom AMM medical dictionary)
symspellpy==6.7.7
# google-cloud-aiplatform installed lazily at inference time (VERTEX_ENDPOINT_ID needed)
# Uncomment when Vertex AI GPU is ready:
# google-cloud-aiplatform==1.65.0
google-cloud-storage==2.18.0
google-cloud-bigquery==3.25.0
google-cloud-kms==2.24.0
cryptography==43.0.1
pydantic==2.8.2
pydantic-settings==2.4.0
# peft removed — not used in worker code
